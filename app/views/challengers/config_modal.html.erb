<%= turbo_frame_tag "config_modal" do %>
  <div class="modal fade" id="configModalModal" tabindex="-1" aria-labelledby="configModalModalLabel" aria-hidden="true" data-controller="config_modal">
    <div class="modal-dialog">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title" id="configModalModalLabel">Configurações</h5>
          <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Fechar"></button>
        </div>
        <div class="modal-body">
          <%= form_with url: challengers_save_config_path do |form| %>
            <div class="modal-body">
              <div class="form-check form-switch">
                <%= check_box_tag 'old', true, switch_old, class: 'form-check-input', id: 'old_switch', data: { action: "config_modal#checkbox" } %>
                <%= label_tag 'old_switch', 'Antigo Testamento', class: 'form-check-label' %>
              </div>
              <br>
              <div class="form-check form-switch">
                <%= check_box_tag 'new', true, switch_new, class: 'form-check-input', id: 'new_switch', data: { action: "config_modal#checkbox" } %>
                <%= label_tag 'new_switch', 'Novo Testamento', class: 'form-check-label' %>
              </div>
            </div>
            <div class="modal-footer">
              <%= submit_tag "Salvar", class: "btn btn-success", data: { action: "config_modal#save_config" } %>
            </div>
          <% end %>
        </div>
      </div>
    </div>
  </div>
<% end %>